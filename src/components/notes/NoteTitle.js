import React from "react";
import NoteDescription from "./NoteDescription";

export default function NoteTitle(params) {
    return (
        <>
            <h1>Spring Boot Foreign Key</h1>
            <NoteDescription/>
        </>
    );
}